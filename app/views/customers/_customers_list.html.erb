<table class="ui very basic collapsing celled padded table" style="width: 100%">
  <tbody>
    <% customers.each do |customer| %>
      <tr>
        <td>
          <h4 class="ui image header">
            <i class="lnr lnr-user"></i>&nbsp;
            <div class="content text-normal">
              <%= link_to customer.name, customer, class: 'text-inherit' %>
              <div class="sub header inline">
                - <%= customer.occupation %>
              </div>
              <div class="sub header">
                <%= customer.address %>
              </div>
            </div>
          </h4>
        </td>
        <td class="center aligned">
          <%= link_to 'Novo negócio', new_rent_path(customer: customer) %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
